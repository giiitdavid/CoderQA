# 測試配置檔案
#  @author <a href="https://github.com/giiitdavid">David</a>
#  </a>
server:
  port: 8101
spring:
  # 資料庫配置
  # todo 需替換配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/coderqa
    username: root
    password: adminroot
  # Redis 配置
  # todo 需替換配置
  redis:
    database: 1
    host: localhost
    port: 6379
    timeout: 5000
    password: 123456
  # Elasticsearch 配置
  # todo 需替換配置
  elasticsearch:
    uris: http://localhost:9200
    username: root
    password: 123456